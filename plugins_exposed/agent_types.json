{
  "agent_types": [
    {
      "agent_type": "hello_world",
      "display_name": "Hello World Agent",
      "description": "Posts the literal text HELLO WORLD once on each simulation round where the agent's activity profile is active.",
      "parameters": [
        {
          "name": "name",
          "type": "string",
          "required": true,
          "description": "Human-readable agent name stored in the YSocial experiment population."
        },
        {
          "name": "activity_profile",
          "type": "string",
          "required": true,
          "description": "Name of a simulation.activity_profiles entry from the client configuration."
        },
        {
          "name": "daily_budget",
          "type": "number",
          "required": true,
          "description": "Daily budget field persisted in the existing experiment population record."
        }
      ]
    },
    {
      "agent_type": "moderator",
      "display_name": "Moderator Agent",
      "description": "Scans recent posts during active slots and can emit moderation actions according to its plugin settings.",
      "parameters": [
        {
          "name": "name",
          "type": "string",
          "required": true,
          "description": "Human-readable agent name stored in the YSocial experiment population."
        },
        {
          "name": "activity_profile",
          "type": "string",
          "required": true,
          "description": "Name of a simulation.activity_profiles entry from the client configuration."
        },
        {
          "name": "daily_budget",
          "type": "number",
          "required": true,
          "description": "Daily budget field persisted in the existing experiment population record."
        },
        {
          "name": "toxicity_keywords",
          "type": "array[string]",
          "required": false,
          "description": "Optional plugin-level keyword list used by the sample moderator implementation."
        }
      ]
    }
  ]
}
